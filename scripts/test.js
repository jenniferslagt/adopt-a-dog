const consoleMessage = () => {
	console.log('The test works!');
}

export { consoleMessage };

// Note: you can only default export one thing in a file
// Use curly brackets for standard exports and imports, not for default exports